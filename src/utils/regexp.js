const regexp = {
    ARITHMETIC_SYMBOLS: /[+\-*/^]/,
    PLUS_MINUS_SYMBOLS: /[+-]/,
    MULT_DIV_EXP_SYMBOLS: /[*/^]/,
    DECIMAL_SYMBOL: /\./,
    DECIMAL_SYMBOL_ENDING: /\.$/
}

export default regexp;
