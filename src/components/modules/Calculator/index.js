import './style.css';

import CalculatorDisplay from '../CalculatorDisplay';

function Calculator() {
    return (
        <div className="calculator">
            <CalculatorDisplay />
        </div>
    );
}

export default Calculator;
